# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'StateAnimator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StateAnimator
  pod 'SnapKit', '~> 3.0'

  target 'StateAnimatorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StateAnimatorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
